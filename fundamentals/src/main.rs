fn is_even(n: i32) -> bool{
    n%2 == 0 //the expression itself is returned its true or false note no ; if we use ;  we return null
}
fn main() {
    let num = 6;
    // let description = if is_even(num){
    //     "even" //no semicolon here
    // }else{
    //     "odd" //no semicolon here
    // };
    // println!("the number {} is {}", num, description);
    // let description = match is_even(num){
    //     true => "even",
    //     false => "odd"
    // };
    // println!("the number {} is {}", num, description);
    // let find = loop{
    //     break 2;
    // };
    // println!("found: {}", find);
}
